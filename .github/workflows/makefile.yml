name: Makefile CI

on:
  push:
    branches: [ "oppo/mt6897_v_15.0.0_reno13_pro" ]
  pull_request:
    branches: [ "oppo/mt6897_v_15.0.0_reno13_pro" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
